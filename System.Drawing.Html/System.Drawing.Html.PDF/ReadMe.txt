This project will render a page as HTML. It is limited to a page at a time (needs pagination support!!) Because the Renderer now 
lives in its own assembly, there's less of a dependency on Winforms.

NB: REQUIRES System.Drawing.Html.Renderer 